# Onboarding module — handles all post-registration setup steps.
# Conditional steps: battery (Off-grid/Hybrid only), grid (On-grid/Hybrid only).
# router imported lazily in main.py to avoid circular import via models/__init__.py
