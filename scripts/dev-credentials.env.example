# Copy to scripts/dev-credentials.env (gitignored) and fill in a local test user.
# Used by ./scripts/dev-up.sh to POST /auth/login and print a dashboard URL.

VOLTA_DEV_EMAIL=you@example.com
VOLTA_DEV_PASSWORD=change-me
